# Partie 1 - Script Bash

Script Bash autonome pour créer les utilisateurs avec toutes les contraintes.

### Fichiers
- `create_users.sh` : Script principal (exécutable)
- `users.txt` : Exemple de fichier utilisateurs

### Utilisation
1. Placer ton `users.txt` dans le dossier
2. Rendre le script exécutable :
   ```bash
   chmod +x create_users.sh
